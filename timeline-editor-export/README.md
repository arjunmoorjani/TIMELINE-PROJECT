# Timeline Editor (Vanilla JS)

## Run
You can open `index.html` directly.

For best compatibility (especially PNG export and loading `data.json`), run a small local server:

```bash
cd "/Users/arjunmoorjani/TIMELINE PROJECT"
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
